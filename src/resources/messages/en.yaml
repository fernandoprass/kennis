AppSettings:
  LoadFinishedSuccess: "Application settings loaded successfully"
Content:
  ParseStart: "Starting content parsing: {content}"
  ParseSuccess: "Content parsing complete: {content}"
  LoadStart: "Starting file load: {file}"
  FileMissing: "Content file not found: {filename}. A new file will be created."
  ReadHeaderFail: "Failed to read Content Header (YAML). Error details: {yaml}"
  DataMissing: "Header is missing in Content file: {file}"
File:
  Missing: "File not found: {filename}"
  ReadFail: "Failed to read file {filename} in folder {folder}"
  ReadSuccess: "File read successfully: {filename}"
Project:
  BuildStart: "Starting project build: {projectName}"
  BuildSuccess: "Project build complete: {projectName}"
  LoadFail: "Failed to load project: {projectName}"
  LoadSuccess: "Project loaded successfully: {projectName}"
  ReadSuccess: "Project file read successfully: {projectName}"
Site:
  BuildStart: "Starting site build for: {languageLabel}"
  BuildSuccess: "Site build complete for: {languageLabel}"
Template:
  FileCopy: "Copying template assets to destination: {siteDestination}"
  FileCopyFailed: "Failed to copy template assets to destination: {error}"
  NotSupported: "Template does not support project's default language ({defaultLanguage}); using {language}"
  LoadStart: "Starting template load: {templateName}"
  LoadFinishedFail: "Failed to load template: {templateName}"
  LoadFinishedSuccess: "Template loaded successfully"
  TranslateSuccess: "Template translation complete: {templateName}"
TranslationFile:
  LoadStart: "Loading i18n data for {language}"
  LoadFinishedFail: "Failed to load i18n data for {language}"
  LoadFinishedSuccess: "I18n data loaded successfully for {language}"
  NotSupported: "Language not supported by template {language}"
HtmlFile:
  ParseSuccess: "HTML page parsed: {pageName}"
  SaveFail: "Error saving HTML file: {filename}"
  SaveSuccess: "HTML file saved successfully: {filename}"
JsonFile:
  ContentDeserializeFail: "Failed to deserialize JSON content. Details: \n{json}"
  DeserializeFail: "Failed to deserialize JSON file: {filename}"
  DeserializeSuccess: "JSON file deserialized successfully: {filename}"
  SaveFail: "Error saving JSON file: {filename}"
  SaveSuccess: "JSON file saved successfully: {filename}"
YamlFile:
  ContentDeserializeFail: "Failed to deserialize YAML content. Details: \n{yaml}"
  DeserializeSuccess: "YAML file deserialized successfully: {filename}"
  ReadFail: "Failed to read YAML Content Header file: {filename}"
Validator:
  Message: "Message: {message}"
  RuleBroken: "Failed during validation: {objectName}"